{
  "name": "sscn-backend",
  "version": "1.0.0",
  "description": "SSCN B2B Supply Chain Marketplace Backend API",
  "scripts": {
    "start": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
    "dev": "uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "test": "pytest",
    "lint": "flake8 app/",
    "format": "black app/"
  },
  "keywords": [
    "fastapi",
    "b2b",
    "supply-chain",
    "marketplace",
    "linkedin",
    "rfq"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}